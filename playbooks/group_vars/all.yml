---

project_admin: user1

home_dir: "{{ playbook_dir }}/.."

# resources dir
resources_base_dir: "{{ home_dir }}/resources"

# oc executable
openshift_cli: oc

#project_name: "{{ project_admin }}-3scale-demo"

monitoring_label: prometheus

# components to install
threescale_install: false # install 3scale (boolean)
amqstreams_install: true # install AMQ streams (boolean)
apicurito_install: false # install API designer (boolean)
codeready_install: false # install CodeReady Workspaces(boolean)
fuseonline_install: false # install fuse online (boolean)
microcks_install: false # install microcks (boolean) - not implemented
mysql_install: true # install MySQL (boolean)
noobaa_install: false # install noobaa (boolean)
sso_install: true # install SSO (boolean)
